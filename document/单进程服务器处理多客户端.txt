基于IO复用，我们可以很容易改进服务器的实现，即，单个服务器进程通过select来管理监听套接字以及连接到服务器的客户端套接字。具体实现为：
	1、将listenfd加入到set中，并将select第一个参数设置为listenfd+1;
	2、循环select,若是listenfd可读，那么将connfd加入到client[]中，并将connfd加入到set中，同时注意select的第一个参数；
	3、若是client[]中的套接字，则直接读取数据，注意如果读取到的字符为0，则表明客户端关闭了连接套接字，此时服务器这边也应该关闭此套接字。